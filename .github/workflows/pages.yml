name: pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: web/app/package-lock.json

      - name: Build the engine as WebAssembly
        run: |
          GOOS=js GOARCH=wasm go build -trimpath -ldflags="-s -w" -o web/app/public/overcut.wasm ./cmd/overcut-wasm
          cp "$(go env GOROOT)/lib/wasm/wasm_exec.js" web/app/public/wasm_exec.js
          mkdir -p web/app/public/data
          cp data/season2026.json web/app/public/data/season.json

      - name: Build the site
        working-directory: web/app
        env:
          VITE_STATIC: "1"
          VITE_BASE: /${{ github.event.repository.name }}/
        run: |
          npm ci
          npm run build -- --outDir ../site --emptyOutDir

      - uses: actions/upload-pages-artifact@v3
        with:
          path: web/site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
